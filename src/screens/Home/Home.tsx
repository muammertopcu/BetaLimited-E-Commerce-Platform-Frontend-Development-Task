import React, {type ReactElement} from 'react';
import {Layout} from '@/components';

function Home(): ReactElement {
    return (
        <Layout>
            <></>
        </Layout>
    );
}

export default Home;
